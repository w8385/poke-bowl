# Poké Bowl Server

현재는 **보류** 상태.

## 판단
- 현 단계 MVP는 Next 정적 사이트만으로 충분함
- 데이터도 시트 -> 정적 JSON 파이프라인으로 먼저 처리
- API, 인증, 관리자 기능이 실제로 필요해질 때만 서버 재검토

## 지금 할 일 아님
- Nest 기능 확장
- Swagger/API 설계
- DB 연결

필요 시 이후 라운드에서 다시 활성화한다.
